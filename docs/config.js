// アイデアBANK の接続設定。ここだけ書き換えてください。
window.IDEA_BANK = {
  // 投稿フォームの送信先（リレーの URL）。デプロイ後に貼り替える。
  // 例: "https://idea-bank-relay.<yourname>.workers.dev"
  endpoint: "",
  // GitHub リポジトリ（owner/repo）。フォールバックのリンク先などに使用。
  repo: "NS-user/NS-idea-BANK",
  // 「組合コード」入力欄を出すか（リレー側 ACCESS_CODE と一致必須にできる）
  requireCode: true,
};
